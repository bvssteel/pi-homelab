---
- name: Configure Raspberry Pi homelab host
  hosts: all
  become: true
  gather_facts: true

  roles:
    - base
    - shell
    - storage
    - docker
    - tailscale
    - updates
    - homelab
